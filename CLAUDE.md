# Rodonit Agro — редизайн (rodonit-agro-redesign)

## Що це і чим відрізняється від rodonit-new

**Два різні проєкти, не плутати:**

| | цей (`rodonit-agro-redesign`) | старий (`rodonit-new`) |
|---|---|---|
| Шлях | `C:\Users\devic\Desktop\rodonit-agro-redesign` | `Desktop\Проєкти\rodonit-new` |
| Стек | Next.js 16.2.12 + Tailwind 4, **контент хардкодом у `lib/`** | Next.js 16 + **Payload CMS + Neon** |
| GitHub | `companyrodonit/Rodonit-agro-new` (акаунт клієнта) | `Lilly0990/rodonit-new` |
| Vercel | team **Rodonit** (акаунт клієнта) + старий `rodonit-redesign` у `hellowebflove-1429` | `devicemarkt-5663` |
| Порт dev | **4002** | 3000 |
| Статус | 🔄 доводимо до здачі | попередня версія, лишається як є |

Дизайн зробила інша людина через Claude Code (коміт `b2e999c`, «Co-Authored-By: Claude Fable 5»).
Олег дизайн **апрувнув** на дзвінку 30.07 («класний, сучасний»), окремо схвалив навігацію по культурах.

⚠️ **Кирилиця в шляху ламає Turbopack.** Проєкт свідомо лежить у `Desktop\`, а не в
`Desktop\Проєкти\` — Turbopack падає з паніком «byte index is not a char boundary»
на українських літерах у шляху. Не переносити в теку з кирилицею.

## План здачі (узгоджено з Бро 10.08.2026)

1. ✅ **Мобільна верстка** — бургер на світлому фоні, фікс дір у картках. Чекає апрув Бро.
2. ✅ **Пуш у клієнтський GitHub → Vercel клієнта** — задеплоєно, всі зміни на
   https://rodonit-agro-new.vercel.app (перевірено curl-ом: логотипи, анкери, чистка заглушок).
3. ✅ **Payload CMS + Postgres** — зроблено 10.08 (деталі нижче). Лишились прод-кроки:
   БД клієнта + env на Vercel + Blob token (потрібні доступи від Бро).
4. ✅ **SEO-аудит + імплементація** — 11.08, деталі в розділі «SEO і GEO» нижче.
5. 🔄 **Переключення на бойовий домен** — розвідка зроблена 11.08, деталі й
   план у **`MIGRATION.md`**. Коротко: домен **rodonit.com.ua** (185.68.16.174,
   DNS `fastdns.hosting` = adm.tools, там же пошта reklama@), старий сайт —
   OpenCart, 174 URL із префіксом `/uk/`, усі 200. Є ще rodonit-agro.com.ua
   (77.120.99.7, controlpanel.host) — інший старий сайт, не наш.
   🔴 **Два блокери:** (1) у GSC підтвердження власності стоїть ЧЕРЕЗ GOOGLE
   ANALYTICS і воно єдине — при заміні сайту відвалиться; (2) аналітики
   фактично немає (на сайті мертва UA-115161662-1, GA4 нема).
   ⚠️ «Зміна адреси» в GSC тут НЕ потрібна — домен той самий, міняється
   лише те, що за ним стоїть.

### Рішення по кроку 3 (щоб не переобговорювати)
- **Hero-карусель НЕ робимо** — ні другого слайда, ні схеми під нього. Hero один,
  **редагований**: заголовок/підзаголовок/фон/кнопки — глобал «Головна сторінка».
- **Новини окремою колекцією НЕ робимо** — рубрики блогу (Статті / Новини).

## Payload CMS (крок 3, зроблено 10.08.2026)

- **Стек**: payload 3.87.1 + @payloadcms/db-postgres + storage-vercel-blob, ESM
  (`"type": "module"`), withPayload у next.config.ts, фронтенд у `app/(frontend)/`,
  адмінка `app/(payload)/` (боілерплейт зі старого rodonit-new).
- **БД для розробки**: Neon Родоніта від rodonit-new, але ОКРЕМА схема **`redesign`**
  (`PAYLOAD_DB_SCHEMA=redesign` у .env.local) — 47 таблиць старого сайту в `public`
  не зачеплені. Прод піде у ВЛАСНУ базу клієнта, схема public (env не задавати).
- **Схема даних**: колекції users, media, categories, products (повна картка:
  specs/регламент/проблеми/SEO), cultures, solutions, distributors, blog-categories,
  posts; глобали home (hero+about+trust+cta) і settings (контакти/меню/юридичне/доставка).
- ⚠️ **Рядки «препарат + норма» живуть у культурах** (cultures.products), НЕ виводяться
  з регламентів препаратів: там культури вільним текстом із синонімами, точний збіг
  23/75. Ці ж рядки живлять фільтр каталогу і лічильники.
- **lib/cms.ts** — шар даних: геттери віддають ТІ САМІ структури, що статичні lib/*;
  БД недоступна → тихий фолбек на статику (білд без env перевірено — зелений).
  Сторінки ISR 300с. Клієнтські компоненти отримують дані пропсами з дефолтами.
- **Сід**: `npm run seed` (tsx --env-file=.env.local scripts/seed.ts) — переливає весь
  хардкод + картинки (упаковки/лого/обкладинки/фон hero) у CMS. Ідемпотентний.
  ⚠️ ПІСЛЯ здачі НЕ ганяти — зітре правки Олега.
- **Адмін**: reklama@rodonit.com.ua / Rodonit-2026-Redesign! (тимчасовий, Олег змінить).
  /admin перевірено браузером: дашборд українською, групи Система/Каталог/Контент/Блог.
- **Медіа**: локально public/uploads (gitignored), на проді Vercel Blob
  (BLOB_READ_WRITE_TOKEN). Без токена аплоад на Vercel не працюватиме (RO fs).
- **Граблі, які вже зловлено**:
  - payload CLI вимагає ESM → `"type": "module"` (CJS-файлів у проєкті не було).
  - Зміна поля колекції → drizzle push питає інтерактивно «create/rename?» і ВИСИТЬ.
    Поки даних мало: `drop schema redesign cascade` + перезапуск dev + `npm run seed`.
    На проді push вимкнено (`push: NODE_ENV !== 'production'`).
  - `robots.ts` у route group тихо випадає зі збірки → зроблено route handler
    `app/(frontend)/robots.txt/route.ts`.
  - Хардкод `rodonit-redesign.vercel.app` лежав у 14 файлах → тепер `lib/site.ts`
    (NEXT_PUBLIC_SITE_URL → VERCEL_PROJECT_PRODUCTION_URL → localhost).
  - Дані, що НЕ в CMS свідомо: тексти /privacy і /terms (авторські юр. документи,
    lib/legal.ts), блок «проблема→препарат» на головній (авторські тексти, lib/content).

### Що треба від Бро для прод-запуску CMS (крок 3-фінал)
1. Neon-база на акаунті клієнта (або дозвіл лишити на нашому) → POSTGRES_URL.
2. На Vercel проєкту rodonit-agro-new: POSTGRES_URL, PAYLOAD_SECRET (новий!),
   NEXT_PUBLIC_SITE_URL, BLOB_READ_WRITE_TOKEN (Storage → Create Blob store).
3. Прогнати `npm run seed` з прод-env (локально, .env тимчасовий) → контент+медіа в проді.

## Зроблено в цій сесії (коміт `c8744b9`)

- **Логотипи дистрибʼюторів** — з `Проєкти\Rodonit\дістр\`, фон зроблено прозорим
  (у НК Рекорд він був кремовий 249,248,244 — читався сірим квадратом), центровані, висота 56px.
- **Лого Родоніт** (атом) у хедері й футері замість спільної іконки-листка.
- **Блок довіри** (`app/trust-art.tsx`) — векторні ілюстрації замість однакових чекмарків;
  на щиті «3 клас небезпечності» цифра «3», а не галочка (щоб не дублювати OMRI-картку).
- **/about**: фото директора (вертикальний 3:4 кроп із препаратами — клієнт обрав саме його),
  колаж препаратів **за реальними пропорціями упаковок** (15/10/8 кг, 5/5/1 л — раніше все
  було однакової висоти й 15-кг мішок виглядав найменшим), команда з 2 людей.
- 🔴 **Прибрано службові тексти, які бачив відвідувач**: «Решта карток чекає на фото від
  замовника», заглушки «Імʼя Прізвище»/«Портрет 4:5», а також нотатки **на /privacy і /terms**
  («потребує уточнення від замовника», «погодити з юристом»). Перевірено всі 60 URL із sitemap.
- **`CtaLink`** (`app/interactive.tsx`) — кнопка «Консультація» була **мертва на 10 сторінках
  із 16**: `href="#cta"`, а секція є не скрізь. Тепер: є секція → плавний скрол; немає →
  перехід на `/contacts#cta`. Додано `id="cta"` на 5 сторінок, де форма була без якоря.
- **Іконки соцмереж** (`app/social-icons.tsx`) замість двобуквених заглушок «FA»/«IN».
- **«Контакти» в головному меню** (не було зовсім) + `nowrap`/`shrink-0` і `.btn-nav`
  padding 19px→12px, бо сьомий пункт ламав хедер на два рядки.
- **Мобільне**: бургер на світлому фоні з лого; `min-h-[400px]`/`[300px]` у картках
  категорій і довіри → тільки від `sm:` (на одноколонковій сітці давали голу діру).


## Правки замовника 10.08.2026 (вечір)
- **Самовивіз прибрано** з блоку «Доставка» (рендериться ТІЛЬКИ на сторінках препаратів,
  `preparaty/[slug]`, під першим екраном). Олег підтвердив: послуги немає, офіс товар не
  видає. Пункт був із референсу ukravit/matador. НЕ ПОВЕРТАТИ. Лишилось 3 способи.
  Решта трьох пунктів (Нова Пошта x2, Укрпошта) досі НЕ підтверджені замовником.
- **Лого НК Рекорд** виглядало найдрібнішим не через CSS: у PNG лишався напівпрозорий
  ореол (альфа 10–128) до краю полотна, і object-contain вписував полотно з ореолом —
  знак займав 86% боксу. Ореол знято, полотно обрізано. Заміряно на проді:
  НК 55x56, Баланс Агро 55x56 — однакові. Агро Мрія 130x56, Biochem 150x52 (горизонтальні).

- **Логотип у хедері — SVG + анімація «електрони по орбітах»** (`app/(frontend)/logo.tsx`,
  стилі `.logo-e` + `@keyframes logo-orbit` у globals.css). Геометрія знята з пікселів PNG:
  4 еліпси 36x17.5 через 45°, ядро — правильний шестикутник R=10.5 (перевірка: розрахунковий
  перетин діагоналей 14.24 проти виміряних 14.5).
  ⚠️ **Кейфрейми ЗГЕНЕРОВАНІ (72 кроки), offset-path НЕ використовувати.** offset-path дає сталу
  швидкість по ДОВЖИНІ ДУГИ — електрон однаково швидко проходить круті кінці еліпса й пологі
  боки, і рух читається як механічне ковзання. Орбіта в проєкції має сталу КУТОВУ швидкість:
  позиція = (rx·cos θ, ry·sin θ) з рівним кроком по θ. Перша версія була саме на offset-path,
  Бро забракував рух як «не схоже на атом» — не повертати.
  Глибина: еліпс = нахилене коло, нижня половина ближча, верхня дальша → на дальній електрон
  менший (scale 0.62→1.02) і тьмяніший (opacity 0.45→1.0). Саме це дає обʼєм.
  Дві орбіти йдуть у зворотному напрямку (`animation-direction: reverse`) — коли всі в один бік,
  знак читався як єдиний вихор. Тривалості зближені й взаємно непарні: 5.6/6.4/6/7.2с.
  `overflow: visible` на SVG ОБОВʼЯЗКОВО — без нього електронам зрізає півкола на кінцях орбіт
  (заміряно семплером на проді: вихід 1.1px з кожного боку, до назви 8px — не заважає).
  Хвости/привиди за електроном — Бро відмовився, не додавати.
  ⚠️ Замінено ТІЛЬКИ хедер. У бургер-шторці і у футері свідомо лишається `/logo.png` —
  пряма вказівка Бро. Не «уніфіковувати» без запиту.
  🔵 Відкрите питання: у референсі електрони проходять ЗА ядром, у нас ні — орбіта в найближчій
  точці на 23.6, ядро R=14.2, тобто електрон завжди йде збоку. Для перекриття треба міняти сам
  знак (орбіти ближче або ядро більше) — Бро ще не вирішив, чи чіпаємо форму.
- **Фавікона досі немає** (у public порожньо). Робити з того ж SVG і ОБОВʼЯЗКОВО до кроку 5:
  Google кешує фавікон під час обходу, на старті без нього у видачі буде сіра заглушка.
  Класти в корінь `app/`, НЕ в `app/(frontend)/` — файлові конвенції в route group випадають.

## SEO і GEO (крок 4, зроблено 11.08.2026)

Аудит: усі 60 сторінок проду завантажено й розібрано по 14 параметрах,
+ заміри швидкості в мобільному вікні 390×844. Звіт —
`Desktop\SEO-Reports\rodonit-agro-seo-audit-2026-08-11.pdf`. Було 58/100.
Коміти `2dde566` (SEO+мобільне) і `d0e7da7` (GEO).

**Було → стало (заміряно краулом, не на око):**
| | до | після |
|---|---|---|
| Сторінок без canonical | 8 (з них головна і всі 6 препаратів) | 0 |
| Сторінок без JSON-LD | 10 | 0 |
| Title коротші за 30 / довші за 60 | 12 / 6 | 0 / 0 |
| Description поза 120–165 | 50 з 60 | 3 |
| URL у sitemap | 60 | 52 (8 тонких тегів закрито) |
| Hero (LCP-елемент), 390px | 711 КБ сирим `<img>` | 34 КБ через next/image |
| Лого дистрибʼюторів | 1326 КБ | 226 КБ |

### Що зроблено і чому саме так
- **Фавікон**: `app/icon.svg` (спрощений знак — 2 орбіти замість 4: на 16px
  чотири злипались у сіру пляму, перевірено рендером у смузі вкладок),
  `app/apple-icon.png` 180 з повним знаком, `app/favicon.ico` 16/32/48.
  ⚠️ Лежать у КОРЕНІ `app/`, і там файлові конвенції працюють — на відміну
  від `robots.ts` усередині route group. Генератор: `scratchpad/make_icons.py`.
- **Canonical**: у кожній сторінці окремо. ⚠️ У root layout ставити НЕ МОЖНА:
  `alternates` успадковується всіма сторінками, які його не задали, і вони
  почали б віддавати canonical на «/». Головна — в `app/(frontend)/page.tsx`.
- **Organization з `@id: SITE + '#org'` + WebSite** — `app/(frontend)/site-schema.tsx`,
  рендериться в layout на кожній сторінці. `/about` і `/contacts` тепер віддають
  AboutPage/ContactPage і посилаються на `#org`, а не описують компанію вдруге.
  **SearchAction свідомо немає**: пошуку по сайту не існує (на /preparaty фільтр
  у межах сторінки — це не те саме), а SearchAction без ендпоінта Google перевіряє.
- **Product + BreadcrumbList** на препаратах. **Offer немає** — цін на сайті немає,
  а Offer із порожньою ціною гірший за відсутність.
- **Article**: `author` (Person із CMS або `#org`), `datePublished` тільки коли дата
  реально є. Дат від замовника досі немає — поле в адмінці чекає.
- **Тег-сторінки**: `TAG_INDEX_THRESHOLD = 3` у `lib/cms.ts`. Менше трьох статей →
  `noindex, follow` + виключення з sitemap через `getIndexableTags()`. Поріг в
  одному місці, бо потрібен і сторінці, і sitemap — інакше «Submitted URL marked
  noindex» у GSC.
- **Нові поля в CMS**, щоб Олег правив сам: `seoTitle` і `metaDescription` у
  препаратах і статтях, `author` у статтях, вкладка «Часті запитання» у препаратах.
- **Швидкість**: hero через `next/image` з `priority` (він LCP-елемент);
  упаковки препаратів через `next/image` з точними 600×790 (усі шість однакові,
  тому пропорція гарантована); лого дистрибʼюторів стиснуті до 224px висоти —
  рендер-розміри не змінились (НК 55×56, Баланс Агро 55×56, заміряно).
  Прибрано 5,9 МБ невикористаних hero-кадрів (лишились в історії git).

### GEO (генеративний пошук)
- `robots.txt` — окремі блоки для GPTBot, OAI-SearchBot, ChatGPT-User,
  PerplexityBot, ClaudeBot, Google-Extended, Applebot-Extended, Bingbot.
  Disallow повторено в КОЖНІЙ секції: частина ботів читає тільки свою.
- `app/(frontend)/llms.txt/route.ts` — карта сайту для AI-агентів, дані з CMS.
- **FAQ**: ✅ **35 питань залито** (6/6/6/6/5/6), замовник апрувнув 11.08.
  Відповіді зібрані з їхніх же опублікованих даних — регламенти, сумісність,
  зберігання, склад; нічого не додумано. Джерело кожного факту зафіксоване
  в артефакті на апрув: https://claude.ai/code/artifact/cc707599-56cc-4441-8363-41b8aa2c3ad4
  Живуть у `lib/products-detail.ts` (фолбек) і в CMS, вкладка «Часті запитання».
  ⚠️ Секція і FAQPage schema рендеряться ТІЛЬКИ коли є хоч одне питання:
  порожній FAQPage Google рахує битою розміткою.
- **WikiData** — `WIKIDATA.md`: що вписувати, звідки джерела, і чому робити
  це треба ПІСЛЯ переїзду на бойовий домен.

### Мобільне (правка Бро 11.08)
Номер телефону рвався посеред цифр («+38 (044) 502-31-» / «56»): значення лежало
голим текстовим вузлом усередині flex, і браузер мав право перенести його по
дефісу. Виправлено в 16 місцях: `whitespace-nowrap` на значенні, `shrink-0` на
іконці, `flex-wrap` на рядку — підпис («— Приймальна секретаря») іде окремим
рядком, номер лишається цілим. Іконки `Phone`/`Mail` тепер приймають `className`.

**Одиниці виміру** — той самий клас проблеми, знайдений уже своїм скриптом:
«1,0–3,0 кг/га» розпадалося на «кг/» і «га», бо коса риска — законне місце
переносу. Було 35 випадків на 13 сторінках, найгустіше в таблицях норм.
Фікс — `lib/typography.ts` → `keepUnits()`: word joiner U+2060 після косої
риски. Не `<span class="whitespace-nowrap">`, бо тексти приходять із CMS
рядками й рендеряться у 8 різних місцях — довелося б усюди розбивати рядок
на масив React-вузлів. ⚠️ Викликати ТІЛЬКИ у видимому тексті: у JSON-LD,
meta description і sitemap має йти чистий рядок. Розрив між ЧИСЛОМ і одиницею
лишено свідомо — це нормальний перенос, а нерозривний пробіл дав би
13-символьний неподільний токен і переповнення колонки.

⚠️ **Скани верстки** — у `tools/`, ганяти після правок верстки (потрібен
піднятий `next start -p 4002`):
- `mobile_scan.py` — усі URL зі sitemap на 390×844: розірвані неподільні
  токени (номер, пошта) через `Range.getClientRects()` на текстовому вузлі —
  інакше розрив у DOM не видно — і горизонтальне переповнення.
- `unit_scan.py` — розриви всередині одиниць виміру, піддіапазоном Range.
- `seo_crawl.py` + `seo_report.py` — краул усіх сторінок і звіт по 14
  SEO-параметрах. Ним заміряні всі цифри «до → після» вище.

Зараз усі чисті: 52 сторінки, 0 проблем.

### Ще не зроблено з аудиту
- **Аналітики немає взагалі** — ні GA4, ні GSC, ні Bing. Це блокер кроку 5:
  без GSC перевірити переїзд неможливо. Верифікацію GSC робити DNS TXT в adm.tools.
- 17 сторінок тонші за 300 слів (переважно культури, 200-290) — це контент у CMS.
- Карти й Google Business Profile на /contacts немає.
- Пошта `info@rodonit-agro.com.ua` на іншому домені, ніж майбутній сайт.

## Команди

```bash
cd /c/Users/devic/Desktop/rodonit-agro-redesign
npm run dev            # http://localhost:4002
npm run build          # має давати 65/65 сторінок без помилок
npx tsc --noEmit       # має бути чисто
```

Перевірка мобільного через agent-browser: `agent-browser set viewport 390 844`
(команда саме `set viewport`, не `viewport` і не `resize`).

## Ключові файли

| Файл | Що всередині |
|---|---|
| `lib/content.ts` | ВЕСЬ контент головної: hero, products, categories, cultures, problems, trust, about, distributors, nav, contacts, footerColumns |
| `lib/posts.ts`, `lib/products-detail.ts` | блог і повні картки препаратів |
| `app/interactive.tsx` | усі клієнтські компоненти: хедер, бургер, слайдер, форма, `CtaLink`, іконки |
| `app/trust-art.tsx`, `app/social-icons.tsx` | векторні ілюстрації довіри / іконки соцмереж |
| `app/globals.css` | змінні палітри (`--color-dark #01362E`, `--color-accent #95E362`), `.btn-nav`, `[id]{scroll-margin-top:104px}` |
| `public/about/`, `public/distributors/`, `public/products/` | фото директора й команди, колаж, лого партнерів, фото препаратів (усі з прозорим фоном) |

## Наступний крок (стан на кінець 11.08.2026, після SEO/GEO і розвідки переїзду)

Порядок жорсткий — кожен наступний пункт залежить від попереднього.
Повний контекст переїзду — `MIGRATION.md`.

1. 🔄 **Бро:** підтвердження власності в GSC другим методом. Зараз воно стоїть
   ТІЛЬКИ через Google Analytics і злетить разом зі старим сайтом.
   Найчистіше — DNS TXT, але домен Бро не делеговано (в adm.tools видно лише
   хостинг), тож треба просити в Олега. Спроба обійтися HTML-файлом на старому
   сайті провалилась: файл лежить у корені, `.htaccess` наявні файли пропускає
   (`!-f`), а Apache його все одно не бачить — розбираємось, підозра на
   розбіжність кореня nginx і Apache (тека `www` від 10.12.2025).
   ⚠️ Не паніка: якщо підтвердження злетить, відновлюємо його вже НАШИМ сайтом
   (Next віддає і файл, і метатег штатно). Розрив на години, не втрата.
2. **Бро:** створити GA4 → дати `G-XXXXXXXXXX`.
3. ✅ **Я:** код GA4 готовий — `app/(frontend)/analytics.tsx`, вмикається
   змінною `NEXT_PUBLIC_GA_ID` на Vercel.
4. **Бро + Олег:** одне питання — чи продаються ще MIRA LIFE S1/S2, Міра ЛИП,
   Ризобакт, Лігногумат. Решта питань закрита (див. п. 6).
5. **Бро:** прод-БД — Neon на акаунті клієнта + env на Vercel.
6. ✅ **Я:** карта редиректів готова — 72 адреси в `lib/redirects.ts`,
   72/72 віддають 301, усі 37 цілей 200.
   🔒 **Стару інфотеку НЕ переносимо — рішення Бро.** 101 адреса свідомо
   лишається на 404. Не піднімати це питання знову і не аналізувати вміст
   старого сайту: новий сайт зібраний зі своїм контентом.
7. **Разом:** домен у Vercel → A-запис → `python tools/check_redirects.py https://rodonit.com.ua`.
8. **Після:** WikiData (`WIKIDATA.md`).

## Інші відкриті питання

Усе закомічено й задеплоєно, робоче дерево чисте, origin/main == HEAD.
Прод: https://rodonit-agro-new.vercel.app

- [ ] **Форма логотипа** — чи міняємо геометрію, щоб електрони проходили за ядром
      (зараз орбіта в найближчій точці 23.6, ядро R=14.2 — не перекриваються).
- [ ] Спитати Олега: решта 3 пунктів доставки (Нова Пошта x2, Укрпошта) досі не підтверджені;
      адреси інших дистрибʼюторів (є лише в Biochem); сайти дистрибʼюторів (у матеріалах немає).
- [ ] Пошта на сайті `info@rodonit-agro.com.ua` — інший домен, ніж майбутній сайт.
- [ ] 17 сторінок тонші за 300 слів (переважно культури) — робота в CMS, не в коді.
- [ ] Карти й Google Business Profile на /contacts немає.

**Закрито 11.08:** мобільна верстка, клієнтський GitHub+Vercel, Payload CMS,
анімований логотип, фавікон, повний SEO+GEO, 35 FAQ, розвідка переїзду.
