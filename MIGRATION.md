# Переїзд на бойовий домен rodonit.com.ua (крок 5)

Стан на 11.08.2026. Усе нижче — заміряні факти зі старого сайту й Search
Console, не припущення. Сирі дані: `tools/old-site.json` (174 URL із
заголовками), чернетка карти редиректів: `tools/redirects-draft.csv`.

---

## Головне непорозуміння, яке треба зняти першим

**Це НЕ переїзд на новий домен.** Домен `rodonit.com.ua` лишається той самий,
міняється тільки те, що за ним стоїть: старий OpenCart → наш Next.js.

Наслідки:
- ресурс у Search Console нікуди не дінеться (він прив'язаний до домену);
- зворотні посилання, вік домену й довіра зберігаються;
- **«Зміна адреси» / Change of Address у GSC подавати НЕ ТРЕБА** — цей
  інструмент лише для переходу між РІЗНИМИ доменами. У попередній редакції
  SEO-звіту я написав інакше, це помилка.

Ламається натомість інше: **URL-адреси** (структура повністю інша) і
**лічильник аналітики** (він живе в HTML старого шаблону).

---

## 🔴 Блокер №1: підтвердження власності в GSC

Перевірено 11.08 на скріні з акаунта клієнта.

- Ресурс: `https://rodonit.com.ua/` — тип **URL-prefix** (не Domain).
- Спосіб підтвердження: **Google Analytics**, і він **єдиний**. Решта
  чотири (HTML-файл, метатег, GTM, DNS) — у списку «Додаткові», тобто
  не підключені.

**Чому це небезпечно.** Верифікація через Google Analytics означає, що Google
щоразу шукає лічильник у HTML головної сторінки. Ми замінюємо сайт цілком —
лічильник зникає — підтвердження злітає — доступ до GSC пропадає рівно тоді,
коли він найпотрібніший (перші дні після переїзду).

**Гірше:** лічильник на сайті — `UA-115161662-1`, тобто **Universal
Analytics**, яку Google вимкнув 01.07.2023 і чиї властивості видалив улітку
2024. Перевірено окремо: GTM немає, GA4 (`G-…`) немає, `analytics.js` немає.
Тобто підтвердження тримається на тезі, за яким уже три роки нічого не стоїть.

**Що зробити ДО перемикання:**
1. GSC → Налаштування → Підтвердження прав власності → «Постачальник
   доменних імен» → отримати TXT-запис.
2. Додати TXT у DNS домену в **adm.tools**.
3. Підтвердити. DNS стане другим методом і переживе заміну сайту.
4. Там же додати окремий ресурс типу **Domain** (`rodonit.com.ua` без схеми) —
   підтвердиться тим самим записом, покриє http/https, www і піддомени.
   Старий URL-prefix ресурс не видаляти.

## Блокер №2: аналітики фактично немає

Стару UA воскресити не можна. Потрібна нова властивість GA4 → ID вимірювання
`G-XXXXXXXXXX` → вставити в код нового сайту. Історії за 2023-2026 немає й
не буде.

Побічно: у GSC «Статистика сканування — дані ще не доступні», а ресурс додано
2026 року. Тобто й у Search Console історії майже немає. Погано для аналізу,
але добре для ризику: втрачати при переїзді практично нічого.

Bing Webmaster Tools після цього імпортується з GSC у два кліки — потрібен,
бо пошук ChatGPT читає індекс Bing.

---

## Що зі старим сайтом

OpenCart. Усі адреси з префіксом **`/uk/`**, частина доступна ще й як
`index.php?route=…`. У robots.txt згадані `/ru/` і `/en/` — у sitemap їх немає,
але вони можуть бути в індексі, це варто перевірити після підключення GSC.

Знято 174 унікальні URL, **усі віддають 200**:

| Розділ | URL | Що в новому сайті | Наших |
|---|---|---|---|
| `diagram` — технологічні схеми | 24 | `/kultury` | 23 |
| `result` — результати застосування | 17 | (окремого розділу немає) | — |
| `problem` — вирішення проблем | 15 | `/rishennia` | 3 |
| `product` — препарати | 11 | `/preparaty` | 7 |
| `category` | 1 | `/preparaty` | 1 |
| `information` — інфотека й новини | 106 | `/blog` | 9 |
| **Разом** | **174** | | **52** |

### Чернетка карти редиректів — `tools/redirects-draft.csv`

| Впевненість | Скільки | Що означає |
|---|---|---|
| точний | 40 | є прямий відповідник, 301 однозначний |
| близький | 14 | «Застосування препаратів на X» → сторінка культури X |
| розділ | 19 | відповідника немає, ведемо на індекс розділу |
| **ПОТРЕБУЄ РІШЕННЯ** | **101** | інфосторінки — вирішує замовник |

**Добра новина.** Технологічні схеми лягають на наші культури майже один в
один: `/uk/kukuruza` → `/kultury/kukurudza`, `/uk/raps` → `/kultury/ripak`
і так далі — 22 з 23. Розділ «Результати застосування» теж лягає на культури:
`/uk/primenenie-na-kukuruze` → `/kultury/kukurudza`. Контент не той самий, але
культура та сама, і це набагато краще, ніж редирект на головну.

**Окремі знахідки:**
- `/uk` (не `/`!) — це сторінка «Про компанію» → `/about`.
- `/uk/zerebra-agro` → `/preparaty/silver-mix` (той самий препарат,
  перейменований).
- `/uk/…` Лігногумат (`product_id=63`) → `/preparaty/mira-rk`: Міра, РК —
  це колишній «Лігногумат БМ».
- **Моркви** в новому сайті немає, а на старому є і схема, і результати.
- Препарати, яких у нас немає: MIRA LIFE S1, MIRA LIFE S2, Міра ЛИП,
  Ризобакт «Гуміфікатор».

### 101 інфосторінка — головне питання до замовника

Це не сміття. Серед них 40+ повноцінних статей на 300–1300 слів: «Відновлення
по розкладу» (1315 сл.), «Перспективний підхід до контролю над хворобами»
(1014), «Передпосівна обробка насіння» (974), «Технологія ТРИ-Е» (960) тощо.
Плюс новини з виставок і серія «Технологія застосування Лігногумату на …».

Частина з них про препарати поза нинішнім портфелем (Лігногумат, Фітолавін,
Альбіт, Ризобакт, МІРА ЛИП) — їх переносити немає сенсу, вони ведуть аграрія
в глухий кут. Але решта — це готовий контент, якого нашому блогу бракує
(зараз 8 статей).

**Питання Олегу — три, і краще одним заходом:**
1. 16 сторінок «Результати застосування» — переносимо як контент чи досить
   редиректу на сторінку культури?
2. Препарати MIRA LIFE S1/S2, Міра ЛИП, Ризобакт, Лігногумат — ще продаються?
   Якщо так, новий сайт неповний і запускати його рано.
3. Зі ~50 змістовних статей інфотеки — які варті переносу в блог?

---

## Порядок робіт

1. **Ти:** DNS-верифікація GSC + Domain-ресурс (блокер №1).
2. **Ти:** створити GA4, дати `G-…`.
3. **Я:** вставити GA4 в код.
4. **Ти + Олег:** рішення по трьох питаннях вище.
5. **Ти:** прод-БД (Neon + env на Vercel) — щоб адмінка працювала на проді.
6. **Я:** доробити карту редиректів під рішення й залити в `next.config.ts`.
7. **Разом:** домен у Vercel → A-запис в adm.tools → я проганяю всі 174 старі
   URL і перевіряю, що кожен віддає 301 куди треба.
8. **Я:** новий sitemap у GSC, Request Indexing на головну й препарати,
   Bing Webmaster.
9. **Після переїзду:** WikiData (`WIKIDATA.md`) — вона чекає бойового домену.

## Інструменти

Усе в `tools/`, запускати з кореня проєкту:

| Файл | Що робить |
|---|---|
| `old_site_crawl.py` | знімає всі URL старого сайту в `old-site.json` |
| `build_redirects.py` | будує `redirects-draft.csv` із зіставленням |
| `seo_crawl.py` + `seo_report.py` | краул нашого сайту й звіт по 14 SEO-параметрах |
| `mobile_scan.py` | розірвані токени й горизонтальне переповнення на 390px |
| `unit_scan.py` | розриви всередині одиниць виміру |
